import authStyle from "@/styles/auth.styles";
import { Text, View } from "react-native";

const Login = () => {
  return (
    <View style={authStyle.container}>
      <Text>Hello There! Welcome to the Login Page</Text>
    </View>
  );
};

export default Login;
